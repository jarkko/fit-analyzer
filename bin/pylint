#!/bin/bash
# Wrapper script for pylint that works with or without venv

set -e

# Try venv first, fall back to system python
if [ -f ".venv/bin/python3" ]; then
    exec .venv/bin/python3 -m pylint "$@"
else
    exec python3 -m pylint "$@"
fi
